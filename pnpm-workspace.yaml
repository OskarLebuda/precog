# https://pnpm.io/settings

packages:
  - playground
  - bench
  - docs

# Always use the local module, never the registry.
overrides:
  nuxt-precog: link:.

dedupePeers: true
ignoreWorkspaceRootCheck: true
